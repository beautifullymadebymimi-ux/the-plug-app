[build]
  command = "pnpm install --frozen-lockfile && npx expo export --platform web --clear"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.4"
  PNPM_VERSION = "9"
  EXPO_NO_TELEMETRY = "1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
